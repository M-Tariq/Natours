{
  "name": "natours",
  "version": "1.0.0",
  "description": "advanced css course project",
  "main": "index.js",
  "scripts": {
    "compile:sass": "node-sass sass/main.scss css/style.css -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "tariq",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^6.0.0"
  }
}
